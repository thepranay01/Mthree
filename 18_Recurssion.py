# when a function call itself repeatedly
""" 
--> Reccursion works on call stack, it is a stack data structure that stores the function calls.

--> When a function is called, it is pushed onto the stack. When the function returns, it is popped off the stack. The call stack is used to keep track of the function calls and their return values.

"""
